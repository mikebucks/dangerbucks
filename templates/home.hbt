{{> header}}

{{{ contents }}}

<ul id="projects">
    {{#each this.projects}}
        <li>
            <a href="{{this.url}}" target="_blank">
                <h3>{{this.title}}</h3>
                <p>{{this.description}}</p>
            </a>
        </li>
    {{/each}}
</ul>

{{> footer}}
